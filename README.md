# MCP Servidor
Proyecto Node.js que actúa como servidor MCP con menú para Web, Mobile y Servicios.

## Cómo ejecutar
1. Instalar dependencias: `npm install`
2. Iniciar servidor: `node server.js`
3. Acceder al menú: `http://localhost:3000/menu`
